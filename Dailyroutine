   day = 01
why we using the sql?
   S = stuctured( organized data)
   Q = query(pre defined syntax)
   L = language(communicacte)

  sql  is used to communicate or interact  with the database.
  To storing and acccessing the data. 
which the places using the sql?
  development,testing,data analytics,datascience,machinelearning,ai, etc.....

   day = 02 
what is the application?
  It is the combination of frontend and backend.
  frontend = send the request to the server.
  backend = server can send to the  database and retrive or store the data.

     day = 03

what is database?
  it is the medium or place to store the data in the organized or systamatic  manner.
  It can perform the CRUD operations.
  C - create and insert, R - retrive(or)read, U - update(or)modify, D - delete(or)drop.
what is data?
  The raw facts which describe the attributes  of an entity.
  entity=object,attributes=properties .

       day = 04

what is DBMS ?
  It is the software used to manage the database.
  It can store the data in the file format.
  The Query language is sed for the communication to the DBMS.
types of DBMS
    Network DBMS = to store the data in the semistructured format.
    Object oriented DBMS = to store the data in the semistructured. 
    Heirarical DBMS = to store the data in the semistructred.
    Relational DBMS = to store the dat in the structured format.
Features:
    security
    authorization
Disadvantages:
    more space consumption.
    more time take to retrive the data in file.
what is RDBMS ?
    It is type of dbms , to store the data in the table format.
    It can use the structured query language for the communication.
    To avoid the disadvantage of DBMS to move on to the RDBMS.
Features:
    security
   Authorization.

     day = 05

HISTORY   OF  RDBMS
 





